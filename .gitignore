# This is a general-purpose .gitignore for a backend project.
# You can uncomment or add lines based on your specific language, framework, and tools.

# --- Build Artifacts ---
# These are files generated by the build process that can be recreated from the source code.
# You should never commit these.
/dist
/build
/target
/out
/bin
/compiled_files
/node_modules
# --- Dependencies / Package Management ---
# The directories where your dependencies are installed. These should be managed
# by your package manager and are not part of the source code.
   # For Node.js projects
/vendor         # For PHP projects (e.g., Composer)
/venv           # For Python projects (virtual environments)
/.pip           # For Python projects (pip cache)
/.gradle        # For Java projects (Gradle build cache)
/.idea/         # For JetBrains IDEs (e.g., Intellij)
/tmp/           # General temporary directory

# --- Logs and Cache ---
# Log files, caches, and other temporary files generated during runtime.
# They are not needed for a clean repository.
*.log
*.tmp
*.bak
npm-debug.log*
yarn-debug.log*
.pnpm-debug.log*
/log
/cache
/.cache

# --- Environment Variables and Secrets ---
# Crucial for security. NEVER commit sensitive information like API keys,
# database credentials, or secret keys. These should be in local environment files.
.env
.env.local
.env.development
.env.production
.env.*.local
*.secrets.json
*.pem
*.key

# --- IDE and Editor Files ---
# Files generated by your IDE or text editor. These are personal to your
# development environment and shouldn't be shared.
.idea/
.vscode/
.DS_Store
Thumbs.db